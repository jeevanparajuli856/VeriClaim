# Local-only placeholders. Keep actual values and Application Default Credentials outside Git.

APP_ENV=local

# Google Gen AI SDK configured for Vertex AI.
GOOGLE_CLOUD_PROJECT=<google-cloud-project-id>
GOOGLE_CLOUD_LOCATION=<vertex-ai-region>
GOOGLE_GENAI_USE_VERTEXAI=true
VERTEX_GEMINI_MODEL=<approved-gemini-model-id>
VERTEX_GEMINI_TIMEOUT_SECONDS=<approved-timeout-seconds>
VERTEX_GEMINI_MAX_OUTPUT_TOKENS=<approved-max-output-tokens>
